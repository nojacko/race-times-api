import { Formula } from "../types/Formula";

export const F1: Formula = { slug: "f1", name: "F1" };
export const F2: Formula = { slug: "f2", name: "F2" };

export const formulas: Formula[] = [F1, F2];

export default formulas;
